// Simple feature flags. Flip these to show/hide work-in-progress features
// without deleting the code.

// ACPD team messaging (/messages). Hidden from the UI for now — the code and
// routes stay intact; set to true to bring it back. The Inbox is separate and
// always on.
export const MESSAGES_ENABLED = false;
