{
  "name": "radius-companion",
  "version": "1.0.0",
  "private": true,
  "description": "Local Mac companion — sends iMessage/SMS from Radius via Messages.app",
  "scripts": {
    "start": "ts-node server.ts"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
